SOFT564Z arduino file to be loaded to the arduino.
ESP32 arduino file to be loaded to the ESP.

Pin assignments as stated in the code, or default for the device.

Connect to the ESP32 on the following access point:

SSID: ESP-32
Password: espaccess

In a web browser, navigate to 198.168.4.1, and use the displayed buttons to control the buggy.

Connect ESP32 to serial (baud rate 115200) to find the address to navigate to for control if this does not work.